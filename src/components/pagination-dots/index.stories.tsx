import * as React from 'react';

import { PaginationDots } from '.';

export default {
  title: 'Pagination',
  parameters: {
    info: { inline: true },
  },
};

export const _PaginationDots = () => (
  <PaginationDots />
);